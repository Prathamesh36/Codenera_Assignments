package DEC1;
/*
import java.util.Scanner;

@FunctionalInterface
interface max{
    public double checkMax();
}
public class CheckNumberIsPrimeOrNotLambda {

    public static void main(String[] args) {

        Scanner sc1=new Scanner(System.in);
        System.out.println("Enter size of an array: ");
        int n=sc1.nextInt();
        double a[]=new double[n];
        for (int i = 0; i < a.length; i++) {
            a[i]=sc1.nextInt();
        }
        max obMax=()->{
            double max=0;
            for (int i = 0; i < a.length; i++) {
                if (a[i]>max) {
                    max=a[i];
                }
            }
            return max;
        };
        System.out.println("Maximum element from an array: "+obMax.checkMax());
    }

}*/